<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Flutter가 로딩될 때 잠깐 보이는 테마 -->
    <style name="LaunchTheme" parent="Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Flutter UI 이후 기본 테마 -->
    <style name="NormalTheme" parent="Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

</resources>
